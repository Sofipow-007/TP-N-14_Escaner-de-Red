package src.main;

import src.controlador.*;

public class Main { // Esta clase se va a utilizar para ejecutar el controlador, por lo que se va a lanzar la interfaz gráfica cuando la ejecutemos
    public static void main(String[] args) {
        
        new Controlador(); // Se llama a la clase Controlador para empezar
        
    }
}
